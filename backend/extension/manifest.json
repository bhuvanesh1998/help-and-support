{
  "manifest_version": 3,
  "name": "HelpAssistant Connector",
  "version": "0.1.0",
  "description": "Connects your logged-in browser to HelpAssistant so Claude can capture screens, APIs and flows. For staging/operator use only.",
  "permissions": ["debugger", "tabs", "storage", "alarms"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js" },
  "action": {
    "default_popup": "popup.html",
    "default_title": "HelpAssistant Connector"
  }
}
